spring:
  application:
    name: api-Travel

  server:
    port: 8080

    #conexion a la base de datos
  datasource:
    url: jdbc:mysql://localhost:3306/db_api_atlastral
    username: root
    password: afps11-mm
    driver-class-name: com.mysql.cj.jdbc.Driver

    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQLDialect


